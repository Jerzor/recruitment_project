# recruitment_project
instalacja paczek zależności:
```npm install``` || ```pnpm i```

uruchamianie projektu:
```npm run dev``` || ```pnpm dev``` 

uruchamianie testów:
```npm run test``` || ```pnpm test```